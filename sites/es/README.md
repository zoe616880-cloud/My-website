# Sitio web en español

独立的西语站点目录，当前本机临时域名：`http://es.asiaweighing.localhost:3102`。

Antes de desplegarlo en el dominio final, reemplace el dominio temporal en `app/layout.tsx` y `app/sitemap.ts`; luego ejecute `npm run build`。
